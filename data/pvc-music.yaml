apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: navidrome
  name: music
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-client
  resources:
    requests:
      storage: 320Gi
